#include <stdio.h>

/**
 *  * main - Entry point
 *  * Description: using the main function
 *  * Return:0 
*/

int main(void)
{
	char n;
	for (n = 'a' ; n <= 'z' ; n++) 
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
